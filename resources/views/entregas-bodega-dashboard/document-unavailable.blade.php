<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Documento no disponible</title>
    <style>
        * { box-sizing: border-box; }
        body { align-items: center; background: #f8fafc; color: #172033; display: flex; font-family: Arial, sans-serif; justify-content: center; margin: 0; min-height: 100vh; padding: 2rem; }
        main { max-width: 32rem; text-align: center; }
        .icon { align-items: center; background: #fff0e8; border-radius: 50%; color: #ec6b2d; display: inline-flex; font-size: 1.75rem; height: 4rem; justify-content: center; width: 4rem; }
        h1 { font-size: 1.2rem; margin: 1rem 0 .65rem; }
        p { color: #5c667a; font-size: .95rem; line-height: 1.55; margin: 0; }
    </style>
</head>
<body>
    <main role="status">
        <div class="icon" aria-hidden="true">!</div>
        <h1>Documento no disponible</h1>
        <p>{{ $message }}</p>
    </main>
</body>
</html>
